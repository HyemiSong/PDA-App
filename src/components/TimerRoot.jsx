import React, {Component} from 'react';
import Timer from "../containers/Timer"
export default class TimerRoot extends Component {
    render(){
        return (
         <Timer></Timer>
        )
    }
}