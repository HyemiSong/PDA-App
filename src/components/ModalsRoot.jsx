import React, {Component} from "react";
import Modals from "../containers/Modals"

export default class ModalsRoot extends Component{
    render(){
        return(
            <div>
                <Modals></Modals>
            </div>
        )
    }
}